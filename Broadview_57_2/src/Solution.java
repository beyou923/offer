public class Solution {
    public int[][] findContinuousSequence(int target){
        if (target < 2) return new int[0][0];
        int small = 1, big = 2;
        int sum = small + big;
        int middle = (small + 1) / 2;
        while (small < middle){
            if (sum == target)
                // 加入二维数组中
            if ()
        }


    }
}
